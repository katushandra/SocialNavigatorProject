@model Domain.DTO.ChangePasswordDto
@{
    ViewData["Title"] = "Смена пароля";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-transparent border-0 pt-4 px-4">
                    <h5 class="fw-bold mb-0">
                        <i class="fi fi-lock me-2"></i>Смена пароля
                    </h5>
                </div>
                <div class="card-body p-4">
                    <form asp-controller="Profile" asp-action="ChangePassword" method="post" class="needs-validation">
                        @Html.AntiForgeryToken()

                        <div class="form-floating mb-3">
                            <input asp-for="CurrentPassword" class="form-control" placeholder="Текущий пароль" type="password" />
                            <label asp-for="CurrentPassword">Текущий пароль</label>
                            <span asp-validation-for="CurrentPassword" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="NewPassword" class="form-control" placeholder="Новый пароль" type="password" />
                            <label asp-for="NewPassword">Новый пароль</label>
                            <span asp-validation-for="NewPassword" class="text-danger small"></span>
                        </div>

                        <div class="form-floating mb-3">
                            <input asp-for="ConfirmPassword" class="form-control" placeholder="Подтвердите пароль" type="password" />
                            <label asp-for="ConfirmPassword">Подтвержите пароль</label>
                            <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                        </div>

                        <div class="row">
                            <div class="col-12 col-md-6 mt-4">
                                <a asp-controller="Profile" asp-action="ProfileHome" class="btn btn-outline-secondary w-100 btn-pill">
                                    <i class="fi fi-arrow-left me-2"></i>Назад
                                </a>
                            </div>

                            <div class="col-12 col-md-6 mt-4 text-align-end text-center-xs">
                                <button type="submit" class="btn btn-primary w-100 btn-pill transition-hover-top">
                                    Сменить пароль
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>